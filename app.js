const heading = React.createElement("h1", { id: "xyem" }, "this is nikhil");
console.log(heading);
const root = ReactDom.createRoot(document.getElementById("root"));
console.log(root);
root.render(heading);
